CREATE TABLE PRODUCT (
      sku VARCHAR(12) NOT NULL,
      name VARCHAR(50) NOT NULL,
      brand VARCHAR(50) NOT NULL,
      size VARCHAR(50) NOT NULL,
      price DOUBLE NOT NULL,
      principal_image VARCHAR(1000)  NOT NULL
);